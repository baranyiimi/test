{% extends 'base.html.twig' %}

{% block title %}Felhasználó
{% endblock %}

{% block body %}
	<h1 class="p-3">Felhasználó</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ user.id }}</td>
			</tr>
			<tr>
				<th>Felhasználó név</th>
				<td>{{ user.username }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-dark" href="{{ path('app_user_index') }}">Vissza a listához</a>

	<a class="btn btn-dark" href="{{ path('app_user_edit', {'id': user.id}) }}">Módosítás</a>

	{{ include('user/_delete_form.html.twig') }}
{% endblock %}
